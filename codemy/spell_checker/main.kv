<MyWidget>:

    BoxLayout:
        orientation: 'vertical'
        size: root.width, root.height
        padding: 30
        spacing: 15

        Label:

        TextInput:

        Button:
            size_hint: (1, .4)
            font_size: 32
            text: 'Submit'